[project]
name = "task-manager"
version = "0.1.0"
description = "Task Manager with FastAPI backend and Streamlit frontend"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.27",
  "streamlit>=1.31",
  "requests>=2.31",
  "pydantic>=2.6",
]

[tool.uv]
dev-dependencies = []